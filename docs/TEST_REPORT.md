# تست منطق

```bash
python3 tests/test_stock_logic.py
```

پوشش: ورود/خروج، موجودی منفی، Quantity، حواله، چند قلم، ویرایش/حذف.

پیام کمبود موجودی در VBA به شکل:

`موجودی این کالا کافی نیست.` + `موجودی فعلی: …`
