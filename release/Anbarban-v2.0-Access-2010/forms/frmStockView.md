# مشخصات frmStockView — مشاهده موجودی انبار

Unbound + زیر‌فرم لیست

| کنترل | متن |
|---|---|
| `txtSearch` | جستجو بر اساس نام یا کد |
| `btnSearch` | جستجو |
| `chkLowOnly` / `btnLowOnly` | نمایش فقط کالاهای کم‌موجودی |
| `btnShowAll` | نمایش همه |
| `btnMovement` | گزارش گردش این کالا |
| `btnBack` | بازگشت به منوی اصلی |
| `subStock` | لیست از `qryStockView` |

ستون‌های لیست: کد، نام، واحد، موجودی فعلی، حداقل، وضعیت (موجود / نیاز به تأمین)  
کد: `vba/frmStockView_Code.bas`
